
$ django-admin startproject avengers
$ cd avengers
$ python3 manage.py runserver
$ python3 manage.py startapp captain_america

"in settings.py add appname in installed apps"
>>>>

# Application definition

INSTALLED_APPS = [
    'django.contrib.admin',
    'django.contrib.auth',
    'django.contrib.contenttypes',
    'django.contrib.sessions',
    'django.contrib.messages',
    'django.contrib.staticfiles',
    'captain_america',
]

<<<

$ cd captain_america

"edit the views.py file and add the following initial code"
>>>

from django.shortcuts import render
from django.http import HttpResponse

# Create your views here.

def index(request):
        return HttpResponse("Captain America - The First Avenger")

<<<

$ cd ../avengers/

"edit the url.py file"
>>>

from django.contrib import admin
from django.urls import path
# add the views file from your app
from captain_america import views

urlpatterns = [
    path('captain_america',views.index,name='index'),
    path('admin/', admin.site.urls),
]

<<<

$ cd ..
$ python3 manage.py runserver

"open http://127.0.0.1:8000/captain_america"

