"in the base directory create a folder templates"
$ mkdir templates

"edit the settings.py"
>>>
#add TEMP_DIR below BASE_DIR
BASE_DIR = os.path.dirname(os.path.dirname(os.path.abspath(__file__)))
TEMP_DIR = os.path.join(BASE_DIR, "templates")
#-------------------------------------------------------------------------
#edit the template as follows
TEMPLATES = [
    {   
        'BACKEND': 'django.template.backends.django.DjangoTemplates',
        #here
        'DIRS': [TEMP_DIR,],
        'APP_DIRS': True,
        'OPTIONS': {
            'context_processors': [
                'django.template.context_processors.debug',
                'django.template.context_processors.request',
                'django.contrib.auth.context_processors.auth',
                'django.contrib.messages.context_processors.messages',
            ],
        },
    },
]

<<<

"in templates folder create a file named index.html"

>>>

<html>
<head>
<title>Captain Marvel</title>
</head>
<body>
Captain Marvel: The Strongest Avenger<br>
{{value}}
</body>
</body>

<<<

"edit the views file of captain_marvel"

>>>

from django.shortcuts import render
from django.http import HttpResponse

# Create your views here.

def index(request):
        my_dict = {'value':  "The Strongest Avenger"}
        return render(request,"index.html",context=my_dict)

<<<
